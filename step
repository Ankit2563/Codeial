npm init-> initialize package
npm install express ->express server
nodemon index.js
npm install ejs

what is cookie ;stores key value pair userId with the seasonal 
cookie basicly stores its identity ocassionally when u logIn
Installing the cookie ->npm install cookie-parser

to got into new branch
some part of the coding have done in the branch manual-local-aut branch
comes to the main branch

Passport is authentication middleware for Node.js. Extremely flexible and modular, Passport can be unobtrusively dropped in to any Express-based web application. A comprehensive set of strategies support authentication using a username and password, Facebook, Twitter, and more.

install passport->npm install passport
install passport-local ->npm install passport-local


to store the cookie in mongo
npm install connect-mongo


SCSS and SASS

Sass is the most mature, stable, and powerful professional grade CSS extension language in the world.

Sass has two syntaxes! The SCSS syntax (.scss) is used most commonly. It’s a superset of CSS, which means all valid CSS is also valid SCSS. The indented syntax (.sass) is more unusual: it uses indentation rather than curly braces to nest statements, and newlines instead of semicolons to separate them. All our examples are available in both syntaxes.

installing sass->npm install node-sass-middleware


